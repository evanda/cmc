// apps/loader — placeholder workspace.
//
// The map-authoring ("loader") tool is a Phase 2 deliverable (plan §10): upload
// satellite imagery, georeference it, digitize building footprints + POIs, and
// place georeferenced interior floor overlays by level. Kept as an empty-but-
// valid workspace in Phase 0 so the monorepo shape (plan §7.1) is complete.

export const LOADER_PHASE = 2 as const;
