// apps/mobile — placeholder workspace.
//
// The React Native (Expo) app is a Phase 4 deliverable (plan §11): requests,
// WO field actions, photos, in-app QR scanning, push, and the MapLibre map in a
// WebView. Kept as an empty-but-valid workspace in Phase 0 so the four-workspace
// monorepo shape (plan §7.1) exists and typechecks/lints cleanly.

export const MOBILE_PHASE = 4 as const;
